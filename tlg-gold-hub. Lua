--====================================================================--
--           TLG HUB 3.0 - COMPACT METALLIC CYAN EDITION              --
--        Mobile Optimized | Arceus X Neo | Delta | Fluxus            --
--====================================================================--

local TS,UI,P,RS,CG,VU,LP = game:GetService("TweenService"),game:GetService("UserInputService"),game:GetService("Players"),game:GetService("RunService"),game:GetService("CoreGui"),game:GetService("VirtualUser"),game.Players.LocalPlayer
local Cam = workspace.CurrentCamera
local V3,VSpd,VDir,PC = false,100,0,nil
pcall(function() local m = require(LP:WaitForChild("PlayerScripts"):WaitForChild("PlayerModule")); PC = m:GetControls() end)

local HAS_DRAWING = false
pcall(function()
    if typeof(Drawing) == "table" and Drawing.new then
        local t = Drawing.new("Square"); t:Remove(); HAS_DRAWING = true
    end
end)

local function GP()
    if gethui then local ok,h = pcall(gethui); if ok and h then return h end end
    local ok = pcall(function() local f = Instance.new("Folder"); f.Parent = CG; f:Destroy() end)
    return ok and CG or LP:WaitForChild("PlayerGui")
end

local SP = GP()
for _,n in ipairs({"TLG_HUB_30","TLG_Fly","TLG_MenuToggle","TLG_Fuga"}) do
    local o = SP:FindFirstChild(n); if o then pcall(function() o:Destroy() end) end
    local o2 = CG:FindFirstChild(n); if o2 then pcall(function() o2:Destroy() end) end
end

----------------------------------------------------------------
-- 🎨 PALETA GRIS METÁLICO + CELESTE NEÓN
----------------------------------------------------------------
local C = {
    BG = Color3.fromRGB(28, 30, 34),
    BG2 = Color3.fromRGB(38, 41, 46),
    BG3 = Color3.fromRGB(50, 54, 60),
    BG4 = Color3.fromRGB(62, 68, 78),
    Accent = Color3.fromRGB(0, 230, 255),
    AccentGlow = Color3.fromRGB(120, 245, 255),
    Text = Color3.fromRGB(240, 245, 250),
    Text2 = Color3.fromRGB(150, 160, 175),
    Text3 = Color3.fromRGB(100, 110, 125),
    Green = Color3.fromRGB(80, 220, 100),
    Red = Color3.fromRGB(240, 70, 80),
    Yellow = Color3.fromRGB(240, 200, 70),
    Metal2 = Color3.fromRGB(80, 88, 100)
}

----------------------------------------------------------------
-- 🪟 VENTANA PRINCIPAL (MÁS PEQUEÑA)
----------------------------------------------------------------
local SG = Instance.new("ScreenGui")
SG.Name = "TLG_HUB_30"
SG.ResetOnSpawn = false
SG.IgnoreGuiInset = true
SG.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
SG.Parent = SP

local Window = Instance.new("Frame", SG)
Window.Name = "Window"
Window.Size = UDim2.new(0, 0, 0, 0)
Window.Position = UDim2.new(0.5, 0, 0.5, 0)
Window.BackgroundColor3 = C.BG
Window.BorderSizePixel = 0
Window.ClipsDescendants = true
Window.Active = true
Instance.new("UICorner", Window).CornerRadius = UDim.new(0, 8)
local WinStroke = Instance.new("UIStroke", Window)
WinStroke.Color = C.Accent
WinStroke.Thickness = 1.5
WinStroke.Transparency = 0.2

local W_SIZE = UDim2.new(0, 450, 0, 340)
local W_POS = UDim2.new(0.5, -225, 0.5, -170)

local function MakeDraggable(obj, handle)
    local drag, ds, sp = false, nil, nil
    handle.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            drag = true; ds = i.Position; sp = obj.Position
            i.Changed:Connect(function() if i.UserInputState == Enum.UserInputState.End then drag = false end end)
        end
    end)
    UI.InputChanged:Connect(function(i)
        if drag and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
            local d = i.Position - ds
            obj.Position = UDim2.new(sp.X.Scale, sp.X.Offset + d.X, sp.Y.Scale, sp.Y.Offset + d.Y)
        end
    end)
end

----------------------------------------------------------------
-- HEADER
----------------------------------------------------------------
local Header = Instance.new("Frame", Window)
Header.Size = UDim2.new(1, 0, 0, 38)
Header.BackgroundColor3 = C.BG2
Header.BorderSizePixel = 0
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 8)
local HeaderCover = Instance.new("Frame", Header)
HeaderCover.Size = UDim2.new(1, 0, 0, 8)
HeaderCover.Position = UDim2.new(0, 0, 1, -8)
HeaderCover.BackgroundColor3 = C.BG2
HeaderCover.BorderSizePixel = 0

local Logo = Instance.new("TextLabel", Header)
Logo.Size = UDim2.new(0, 30, 0, 30)
Logo.Position = UDim2.new(0, 10, 0.5, -15)
Logo.BackgroundTransparency = 1
Logo.Font = Enum.Font.GothamBold
Logo.Text = "⚡"
Logo.TextColor3 = C.Accent
Logo.TextSize = 18

task.spawn(function()
    while Logo.Parent do
        TS:Create(Logo, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {TextColor3 = C.AccentGlow}):Play()
        task.wait(1.5)
        if not Logo.Parent then break end
        TS:Create(Logo, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {TextColor3 = C.Accent}):Play()
        task.wait(1.5)
    end
end)

local Title = Instance.new("TextLabel", Header)
Title.Size = UDim2.new(0, 180, 1, 0)
Title.Position = UDim2.new(0, 44, 0, 0)
Title.BackgroundTransparency = 1
Title.Font = Enum.Font.GothamBold
Title.Text = "TLG HUB 3.0"
Title.TextColor3 = C.Text
Title.TextSize = 13
Title.TextXAlignment = Enum.TextXAlignment.Left

local Version = Instance.new("TextLabel", Header)
Version.Size = UDim2.new(0, 80, 1, 0)
Version.Position = UDim2.new(1, -120, 0, 0)
Version.BackgroundTransparency = 1
Version.Font = Enum.Font.GothamMedium
Version.Text = "TLG Team"
Version.TextColor3 = C.Text2
Version.TextSize = 10
Version.TextXAlignment = Enum.TextXAlignment.Right

local CloseBtn = Instance.new("TextButton", Header)
CloseBtn.Size = UDim2.new(0, 28, 0, 28)
CloseBtn.Position = UDim2.new(1, -34, 0.5, -14)
CloseBtn.BackgroundColor3 = C.BG3
CloseBtn.Text = "✕"
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextColor3 = C.Red
CloseBtn.TextSize = 14
CloseBtn.BorderSizePixel = 0
CloseBtn.AutoButtonColor = false
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 5)

MakeDraggable(Window, Header)

----------------------------------------------------------------
-- TAB BAR
----------------------------------------------------------------
local TabBar = Instance.new("Frame", Window)
TabBar.Size = UDim2.new(1, -16, 0, 30)
TabBar.Position = UDim2.new(0, 8, 0, 44)
TabBar.BackgroundColor3 = C.BG2
TabBar.BorderSizePixel = 0
Instance.new("UICorner", TabBar).CornerRadius = UDim.new(0, 5)

local TabScroll = Instance.new("ScrollingFrame", TabBar)
TabScroll.Size = UDim2.new(1, -4, 1, 0)
TabScroll.Position = UDim2.new(0, 2, 0, 0)
TabScroll.BackgroundTransparency = 1
TabScroll.BorderSizePixel = 0
TabScroll.ScrollBarThickness = 0
TabScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
pcall(function() TabScroll.AutomaticCanvasSize = Enum.AutomaticSize.X end)
pcall(function() TabScroll.ScrollingDirection = Enum.ScrollingDirection.X end)
local TabList = Instance.new("UIListLayout", TabScroll)
TabList.FillDirection = Enum.FillDirection.Horizontal
TabList.VerticalAlignment = Enum.VerticalAlignment.Center
TabList.HorizontalAlignment = Enum.HorizontalAlignment.Left
TabList.Padding = UDim.new(0, 3)

----------------------------------------------------------------
-- CONTENT
----------------------------------------------------------------
local Content = Instance.new("ScrollingFrame", Window)
Content.Size = UDim2.new(1, -16, 1, -130)
Content.Position = UDim2.new(0, 8, 0, 80)
Content.BackgroundTransparency = 1
Content.BorderSizePixel = 0
Content.ScrollBarThickness = 3
Content.ScrollBarImageColor3 = C.Accent
Content.CanvasSize = UDim2.new(0, 0, 0, 0)
pcall(function() Content.AutomaticCanvasSize = Enum.AutomaticSize.Y end)
local ContentList = Instance.new("UIListLayout", Content)
ContentList.Padding = UDim.new(0, 6)

----------------------------------------------------------------
-- FOOTER
----------------------------------------------------------------
local Footer = Instance.new("Frame", Window)
Footer.Size = UDim2.new(1, -16, 0, 26)
Footer.Position = UDim2.new(0, 8, 1, -32)
Footer.BackgroundColor3 = C.BG2
Footer.BorderSizePixel = 0
Instance.new("UICorner", Footer).CornerRadius = UDim.new(0, 5)

local FooterText = Instance.new("TextLabel", Footer)
FooterText.Size = UDim2.new(1, -16, 1, 0)
FooterText.Position = UDim2.new(0, 8, 0, 0)
FooterText.BackgroundTransparency = 1
FooterText.Font = Enum.Font.GothamMedium
FooterText.Text = "Arceus X Neo / Delta / Fluxus  •  PC + Móvil"
FooterText.TextColor3 = C.Text2
FooterText.TextSize = 9
FooterText.TextXAlignment = Enum.TextXAlignment.Left

----------------------------------------------------------------
-- TABS SYSTEM
----------------------------------------------------------------
local Tabs = {}
local CurrentTab = nil

local function CreateTab(name)
    local btn = Instance.new("TextButton", TabScroll)
    btn.Size = UDim2.new(0, 78, 1, -4)
    btn.Position = UDim2.new(0, 0, 0, 2)
    btn.BackgroundColor3 = C.BG3
    btn.Text = name
    btn.Font = Enum.Font.GothamSemibold
    btn.TextColor3 = C.Text2
    btn.TextSize = 11
    btn.BorderSizePixel = 0
    btn.AutoButtonColor = false
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 4)
    
    local page = Instance.new("Frame", Content)
    page.Size = UDim2.new(1, -4, 0, 0)
    page.BackgroundTransparency = 1
    page.Visible = false
    page.AutomaticSize = Enum.AutomaticSize.Y
    local pl = Instance.new("UIListLayout", page)
    pl.Padding = UDim.new(0, 6)
    
    table.insert(Tabs, {btn = btn, page = page, name = name})
    
    btn.MouseButton1Click:Connect(function()
        for _, t in ipairs(Tabs) do
            t.btn.BackgroundColor3 = C.BG3
            t.btn.TextColor3 = C.Text2
            t.page.Visible = false
        end
        btn.BackgroundColor3 = C.Accent
        btn.TextColor3 = C.BG
        page.Visible = true
        CurrentTab = name
    end)
    
    return page
end

----------------------------------------------------------------
-- ELEMENTOS UI (MÁS COMPACTOS)
----------------------------------------------------------------
local function CreateSection(parent, title)
    local section = Instance.new("Frame", parent)
    section.Size = UDim2.new(1, 0, 0, 26)
    section.BackgroundColor3 = C.BG2
    section.BorderSizePixel = 0
    Instance.new("UICorner", section).CornerRadius = UDim.new(0, 5)
    
    local accentBar = Instance.new("Frame", section)
    accentBar.Size = UDim2.new(0, 3, 0, 14)
    accentBar.Position = UDim2.new(0, 8, 0.5, -7)
    accentBar.BackgroundColor3 = C.Accent
    accentBar.BorderSizePixel = 0
    Instance.new("UICorner", accentBar).CornerRadius = UDim.new(0, 2)
    
    local lbl = Instance.new("TextLabel", section)
    lbl.Size = UDim2.new(1, -22, 1, 0)
    lbl.Position = UDim2.new(0, 18, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Font = Enum.Font.GothamBold
    lbl.Text = title
    lbl.TextColor3 = C.Text
    lbl.TextSize = 11
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    
    return section
end

local function CreateToggle(parent, text, default, callback)
    local frame = Instance.new("Frame", parent)
    frame.Size = UDim2.new(1, 0, 0, 34)
    frame.BackgroundColor3 = C.BG2
    frame.BorderSizePixel = 0
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 5)
    
    local cb = Instance.new("Frame", frame)
    cb.Size = UDim2.new(0, 16, 0, 16)
    cb.Position = UDim2.new(0, 8, 0.5, -8)
    cb.BackgroundColor3 = default and C.Accent or C.BG3
    cb.BorderSizePixel = 0
    Instance.new("UICorner", cb).CornerRadius = UDim.new(0, 3)
    local cbStroke = Instance.new("UIStroke", cb)
    cbStroke.Color = C.Accent
    cbStroke.Thickness = 1
    cbStroke.Transparency = default and 1 or 0.5
    
    local check = Instance.new("TextLabel", cb)
    check.Size = UDim2.new(1, 0, 1, 0)
    check.BackgroundTransparency = 1
    check.Font = Enum.Font.GothamBold
    check.Text = "✓"
    check.TextColor3 = C.BG
    check.TextSize = 11
    check.Visible = default
    
    local lbl = Instance.new("TextLabel", frame)
    lbl.Size = UDim2.new(1, -70, 1, 0)
    lbl.Position = UDim2.new(0, 32, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Font = Enum.Font.GothamMedium
    lbl.Text = text
    lbl.TextColor3 = C.Text
    lbl.TextSize = 11
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.TextTruncate = Enum.TextTruncate.AtEnd
    
    local colorSq = Instance.new("Frame", frame)
    colorSq.Size = UDim2.new(0, 20, 0, 14)
    colorSq.Position = UDim2.new(1, -28, 0.5, -7)
    colorSq.BackgroundColor3 = default and C.Accent or C.Metal2
    colorSq.BorderSizePixel = 0
    Instance.new("UICorner", colorSq).CornerRadius = UDim.new(0, 3)
    
    local state = default
    local clickBtn = Instance.new("TextButton", frame)
    clickBtn.Size = UDim2.new(1, 0, 1, 0)
    clickBtn.BackgroundTransparency = 1
    clickBtn.Text = ""
    clickBtn.MouseButton1Click:Connect(function()
        state = not state
        if state then
            cb.BackgroundColor3 = C.Accent
            cbStroke.Transparency = 1
            check.Visible = true
            colorSq.BackgroundColor3 = C.Accent
        else
            cb.BackgroundColor3 = C.BG3
            cbStroke.Transparency = 0.5
            check.Visible = false
            colorSq.BackgroundColor3 = C.Metal2
        end
        pcall(callback, state)
    end)
    
    return frame
end

local function CreateSlider(parent, text, min, max, default, callback)
    local frame = Instance.new("Frame", parent)
    frame.Size = UDim2.new(1, 0, 0, 44)
    frame.BackgroundColor3 = C.BG2
    frame.BorderSizePixel = 0
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 5)
    
    local lbl = Instance.new("TextLabel", frame)
    lbl.Size = UDim2.new(0.6, -16, 0, 16)
    lbl.Position = UDim2.new(0, 10, 0, 4)
    lbl.BackgroundTransparency = 1
    lbl.Font = Enum.Font.GothamMedium
    lbl.Text = text
    lbl.TextColor3 = C.Text
    lbl.TextSize = 10
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.TextTruncate = Enum.TextTruncate.AtEnd
    
    local valLbl = Instance.new("TextLabel", frame)
    valLbl.Size = UDim2.new(0.4, -16, 0, 16)
    valLbl.Position = UDim2.new(0.6, 6, 0, 4)
    valLbl.BackgroundTransparency = 1
    valLbl.Font = Enum.Font.GothamBold
    valLbl.Text = tostring(default)
    valLbl.TextColor3 = C.Accent
    valLbl.TextSize = 10
    valLbl.TextXAlignment = Enum.TextXAlignment.Right
    
    local bar = Instance.new("Frame", frame)
    bar.Size = UDim2.new(1, -20, 0, 5)
    bar.Position = UDim2.new(0, 10, 0, 26)
    bar.BackgroundColor3 = C.BG3
    bar.BorderSizePixel = 0
    Instance.new("UICorner", bar).CornerRadius = UDim.new(1, 0)
    
    local fill = Instance.new("Frame", bar)
    fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
    fill.BackgroundColor3 = C.Accent
    fill.BorderSizePixel = 0
    Instance.new("UICorner", fill).CornerRadius = UDim.new(1, 0)
    
    local knob = Instance.new("Frame", fill)
    knob.Size = UDim2.new(0, 10, 0, 10)
    knob.Position = UDim2.new(1, -5, 0.5, -5)
    knob.BackgroundColor3 = C.Text
    knob.BorderSizePixel = 0
    Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)
    
    local touch = Instance.new("TextButton", frame)
    touch.Size = UDim2.new(1, -16, 0, 34)
    touch.Position = UDim2.new(0, 8, 0, 10)
    touch.BackgroundTransparency = 1
    touch.Text = ""
    
    local dragging = false
    local function update(i)
        local p = math.clamp((i.Position.X - bar.AbsolutePosition.X) / bar.AbsoluteSize.X, 0, 1)
        local v = math.floor(min + (max - min) * p)
        fill.Size = UDim2.new(p, 0, 1, 0)
        valLbl.Text = tostring(v)
        pcall(callback, v)
    end
    
    touch.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            dragging = true; update(i)
        end
    end)
    UI.InputChanged:Connect(function(i)
        if dragging and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
            update(i)
        end
    end)
    UI.InputEnded:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)
    
    return frame
end

local function CreateButton(parent, text, callback)
    local btn = Instance.new("TextButton", parent)
    btn.Size = UDim2.new(1, 0, 0, 32)
    btn.BackgroundColor3 = C.BG3
    btn.Font = Enum.Font.GothamSemibold
    btn.Text = text
    btn.TextColor3 = C.Text
    btn.TextSize = 11
    btn.BorderSizePixel = 0
    btn.AutoButtonColor = false
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 5)
    local stroke = Instance.new("UIStroke", btn)
    stroke.Color = C.Accent
    stroke.Thickness = 1
    stroke.Transparency = 0.7
    
    btn.MouseEnter:Connect(function() TS:Create(stroke, TweenInfo.new(0.15), {Transparency = 0}):Play() end)
    btn.MouseLeave:Connect(function() TS:Create(stroke, TweenInfo.new(0.15), {Transparency = 0.7}):Play() end)
    btn.MouseButton1Click:Connect(function() pcall(callback) end)
    
    return btn
end

local function CreateLabel(parent, text, color)
    local lbl = Instance.new("TextLabel", parent)
    lbl.Size = UDim2.new(1, 0, 0, 18)
    lbl.BackgroundTransparency = 1
    lbl.Font = Enum.Font.GothamMedium
    lbl.Text = text
    lbl.TextColor3 = color or C.Text2
    lbl.TextSize = 10
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.TextWrapped = true
    lbl.Position = UDim2.new(0, 4, 0, 0)
    return lbl
end

----------------------------------------------------------------
-- VARIABLES
----------------------------------------------------------------
local SHon, SHv = false, 100
local FAon, FAs = false, 15
local EH = 10000
local HBCelesteOn, HBCelesteSize = false, 10
local HBCelesteConn = nil
local HBGiantOn, HBGiantSize = false, 60000
local HBGiantConn = nil
local HBGiantCache = {}
local GOT, GOC, GOd, GOs, GOh, GOang = nil, nil, 5, 3, 0, 0
local ESPon = false
local ESPboxes = {}

----------------------------------------------------------------
-- CREAR TABS
----------------------------------------------------------------
local FlyPage = CreateTab("Fly")
local SpeedPage = CreateTab("Speed")
local CombatPage = CreateTab("Combat")
local HitboxPage = CreateTab("Hitbox")
local OrbitPage = CreateTab("Orbit")
local ToolPage = CreateTab("Herramientas")
local CreditPage = CreateTab("Créditos")

if Tabs[1] then
    Tabs[1].btn.BackgroundColor3 = C.Accent
    Tabs[1].btn.TextColor3 = C.BG
    Tabs[1].page.Visible = true
end

----------------------------------------------------------------
-- FLY TAB
----------------------------------------------------------------
CreateSection(FlyPage, "Fly V3")
CreateToggle(FlyPage, "Activar Fly", false, function(v)
    if v then StartFlyInternal() else StopFlyInternal() end
end)
CreateSlider(FlyPage, "Velocidad de Vuelo", 10, 500, 100, function(v) VSpd = v end)
CreateToggle(FlyPage, "Mostrar Botón Flotante", false, function(v)
    FB.Visible = v
    if not v then UB.Visible = false; DB.Visible = false end
    if v and V3 then UB.Visible = true; DB.Visible = true end
end)
CreateLabel(FlyPage, "• Mueve con joystick (móvil) o WASD (PC)", C.Text2)
CreateLabel(FlyPage, "• Botones ↑/↓ para subir y bajar", C.Text2)

----------------------------------------------------------------
-- SPEED TAB (SIN DASH)
----------------------------------------------------------------
CreateSection(SpeedPage, "Speedhack")
CreateToggle(SpeedPage, "Activar Speedhack", false, function(v) SHon = v end)
CreateSlider(SpeedPage, "Velocidad del Personaje", 16, 500, 100, function(v) SHv = v end)

----------------------------------------------------------------
-- COMBAT TAB
----------------------------------------------------------------
CreateSection(CombatPage, "Fast Attack")
CreateToggle(CombatPage, "Activar Fast Attack", false, function(v)
    FAon = v
    if not v then return end
    task.spawn(function()
        while FAon do
            pcall(function() VU:ClickButton1(Vector2.new(0,0)) end)
            pcall(function()
                local t = LP.Character and LP.Character:FindFirstChildOfClass("Tool")
                if t then t:Activate() end
            end)
            task.wait(1 / FAs)
        end
    end)
end)
CreateSlider(CombatPage, "Clicks por segundo", 5, 40, 15, function(v) FAs = v end)

CreateSection(CombatPage, "Fuga de Emergencia")
CreateButton(CombatPage, "🚀 Ejecutar Fuga", function()
    local c = LP.Character
    local h = c and c:FindFirstChild("HumanoidRootPart")
    if h then h.CFrame = h.CFrame + Vector3.new(0, EH, 0) end
end)
CreateSlider(CombatPage, "Altura de Fuga", 1000, 50000, 10000, function(v) EH = v end)
CreateToggle(CombatPage, "🛡 Botón Flotante de Fuga", false, function(v)
    FugaBtn.Visible = v
end)

----------------------------------------------------------------
-- HITBOX TAB
----------------------------------------------------------------
CreateSection(HitboxPage, "Hitbox Celeste")
CreateToggle(HitboxPage, "🔵 Activar Hitbox Celeste", false, function(v)
    HBCelesteOn = v
    if v then StartCeleste() else StopCeleste() end
end)
CreateSlider(HitboxPage, "Tamaño Celeste", 2, 50, 10, function(v) HBCelesteSize = v end)

CreateSection(HitboxPage, "Hitbox Gigante")
CreateToggle(HitboxPage, "🔴 Activar Hitbox Gigante", false, function(v)
    HBGiantOn = v
    if v then StartGiant() else StopGiant() end
end)
CreateSlider(HitboxPage, "Tamaño Gigante", 1000, 100000, 60000, function(v) HBGiantSize = v end)
CreateLabel(HitboxPage, "⚠️ No actives ambas al mismo tiempo", C.Yellow)

----------------------------------------------------------------
-- ORBIT TAB
----------------------------------------------------------------
CreateSection(OrbitPage, "God Orb")
CreateLabel(OrbitPage, "Selecciona un jugador", C.Text2)

local PlayerListFrame = Instance.new("Frame", OrbitPage)
PlayerListFrame.Size = UDim2.new(1, 0, 0, 150)
PlayerListFrame.BackgroundColor3 = C.BG2
PlayerListFrame.BorderSizePixel = 0
Instance.new("UICorner", PlayerListFrame).CornerRadius = UDim.new(0, 5)

local PlayerScroll = Instance.new("ScrollingFrame", PlayerListFrame)
PlayerScroll.Size = UDim2.new(1, -6, 1, -6)
PlayerScroll.Position = UDim2.new(0, 3, 0, 3)
PlayerScroll.BackgroundTransparency = 1
PlayerScroll.BorderSizePixel = 0
PlayerScroll.ScrollBarThickness = 3
PlayerScroll.ScrollBarImageColor3 = C.Accent
PlayerScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
pcall(function() PlayerScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y end)
local PLList = Instance.new("UIListLayout", PlayerScroll)
PLList.Padding = UDim.new(0, 3)

function PopulatePlayerList()
    pcall(function()
        for _, c in ipairs(PlayerScroll:GetChildren()) do
            if c:IsA("TextButton") then c:Destroy() end
        end
        for _, plr in ipairs(P:GetPlayers()) do
            if plr ~= LP then
                local b = Instance.new("TextButton", PlayerScroll)
                b.Size = UDim2.new(1, -4, 0, 26)
                b.BackgroundColor3 = C.BG3
                b.Font = Enum.Font.GothamMedium
                b.Text = "🌀 " .. plr.DisplayName
                b.TextColor3 = C.Text
                b.TextSize = 10
                b.BorderSizePixel = 0
                b.AutoButtonColor = false
                Instance.new("UICorner", b).CornerRadius = UDim.new(0, 4)
                
                b.MouseButton1Click:Connect(function()
                    if GOT == plr then
                        StopGO()
                        b.BackgroundColor3 = C.BG3
                        b.Text = "🌀 " .. plr.DisplayName
                    else
                        StartGO(plr)
                        for _, other in ipairs(PlayerScroll:GetChildren()) do
                            if other:IsA("TextButton") and other ~= b then
                                other.BackgroundColor3 = C.BG3
                                other.Text = other.Text:gsub("✅ ", "🌀 ")
                            end
                        end
                        b.BackgroundColor3 = C.Accent
                        b.TextColor3 = C.BG
                        b.Text = "✅ " .. plr.DisplayName
                    end
                end)
            end
        end
    end)
end

CreateSection(OrbitPage, "Controles")
CreateSlider(OrbitPage, "Distancia", 2, 30, 5, function(v) GOd = v end)
CreateSlider(OrbitPage, "Velocidad", 1, 20, 3, function(v) GOs = v end)
CreateSlider(OrbitPage, "Altura Extra", 0, 20, 0, function(v) GOh = v end)
CreateButton(OrbitPage, "🛑 Detener God Orb", function() StopGO(); PopulatePlayerList() end)

----------------------------------------------------------------
-- 🆕 HERRAMIENTAS TAB (CON ESP)
----------------------------------------------------------------
CreateSection(ToolPage, "ESP de Jugadores")

if HAS_DRAWING then
    CreateToggle(ToolPage, "👁 Activar ESP (Nombre + HP)", false, function(v)
        ESPon = v
        if v then
            -- Crear cajas para todos los jugadores
            for _, plr in ipairs(P:GetPlayers()) do
                if plr ~= LP and not ESPboxes[plr] then
                    local ok, box, txt = pcall(function()
                        local b = Drawing.new("Square")
                        b.Thickness = 1.5
                        b.Filled = false
                        b.Visible = false
                        b.Color = C.Accent
                        local t = Drawing.new("Text")
                        t.Size = 13
                        t.Center = true
                        t.Outline = true
                        t.OutlineColor = Color3.fromRGB(0, 0, 0)
                        t.Color = C.AccentGlow
                        t.Visible = false
                        return b, t
                    end)
                    if ok then ESPboxes[plr] = {box = box, txt = txt} end
                end
            end
        else
            for _, d in pairs(ESPboxes) do
                if d.box then pcall(function() d.box.Visible = false end) end
                if d.txt then pcall(function() d.txt.Visible = false end) end
            end
        end
    end)

    CreateToggle(ToolPage, "📏 Mostrar Distancia", true, function(v)
        ESP_ShowDist = v
    end)

    CreateSlider(ToolPage, "🎨 Grosor del ESP", 1, 5, 1, function(v)
        for _, d in pairs(ESPboxes) do
            if d.box then pcall(function() d.box.Thickness = v end) end
        end
    end)

    -- Loop del ESP
    RS.RenderStepped:Connect(function()
        if not ESPon then return end
        for plr, d in pairs(ESPboxes) do
            pcall(function()
                local char = plr.Character
                local hrp = char and char:FindFirstChild("HumanoidRootPart")
                local hum = char and char:FindFirstChildOfClass("Humanoid")
                if char and hrp and hum and hum.Health > 0 then
                    local _, onScreen = Cam:WorldToViewportPoint(hrp.Position)
                    if onScreen then
                        local rp = Cam:WorldToViewportPoint(hrp.Position)
                        local hp = Cam:WorldToViewportPoint(hrp.Position + Vector3.new(0, 3, 0))
                        local lp = Cam:WorldToViewportPoint(hrp.Position - Vector3.new(0, 3.5, 0))
                        local hh = math.abs(hp.Y - lp.Y)
                        local w = hh / 1.8
                        d.box.Size = Vector2.new(w, hh)
                        d.box.Position = Vector2.new(rp.X - w/2, rp.Y - hh/2)
                        d.box.Visible = true
                        
                        local myHrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
                        local dist = myHrp and math.floor((hrp.Position - myHrp.Position).Magnitude) or 0
                        local txtStr = plr.DisplayName .. " | " .. math.floor(hum.Health) .. " HP"
                        if ESP_ShowDist ~= false then txtStr = txtStr .. " | [" .. dist .. "m]" end
                        d.txt.Text = txtStr
                        d.txt.Position = Vector2.new(rp.X, rp.Y - hh/2 - 16)
                        d.txt.Visible = true
                    else
                        d.box.Visible = false
                        d.txt.Visible = false
                    end
                else
                    d.box.Visible = false
                    d.txt.Visible = false
                end
            end)
        end
    end)

    -- Crear ESP cuando entra un jugador nuevo
    P.PlayerAdded:Connect(function(plr)
        if ESPon and plr ~= LP and not ESPboxes[plr] then
            local ok, box, txt = pcall(function()
                local b = Drawing.new("Square")
                b.Thickness = 1.5
                b.Filled = false
                b.Visible = false
                b.Color = C.Accent
                local t = Drawing.new("Text")
                t.Size = 13
                t.Center = true
                t.Outline = true
                t.OutlineColor = Color3.fromRGB(0, 0, 0)
                t.Color = C.AccentGlow
                t.Visible = false
                return b, t
            end)
            if ok then ESPboxes[plr] = {box = box, txt = txt} end
        end
    end)

    -- Limpiar cuando sale un jugador
    P.PlayerRemoving:Connect(function(plr)
        if ESPboxes[plr] then
            if ESPboxes[plr].box then pcall(function() ESPboxes[plr].box:Remove() end) end
            if ESPboxes[plr].txt then pcall(function() ESPboxes[plr].txt:Remove() end) end
            ESPboxes[plr] = nil
        end
    end)

    CreateLabel(ToolPage, "• Muestra cajas celestes con nombre, HP y distancia", C.Text2)
else
    CreateLabel(ToolPage, "⚠️ ESP no disponible: tu ejecutor no soporta Drawing", C.Yellow)
    CreateLabel(ToolPage, "• Prueba con Delta, Arceus X Neo o Fluxus", C.Text2)
end

CreateSection(ToolPage, "Información")
CreateLabel(ToolPage, "• El ESP se actualiza automáticamente", C.Text2)
CreateLabel(ToolPage, "• Color celeste neón como el resto del HUB", C.Text2)

----------------------------------------------------------------
-- CRÉDITOS TAB
----------------------------------------------------------------
local CreditHeader = Instance.new("Frame", CreditPage)
CreditHeader.Size = UDim2.new(1, 0, 0, 110)
CreditHeader.BackgroundColor3 = C.BG2
CreditHeader.BorderSizePixel = 0
Instance.new("UICorner", CreditHeader).CornerRadius = UDim.new(0, 6)
Instance.new("UIStroke", CreditHeader).Color = C.Accent

local Crown = Instance.new("TextLabel", CreditHeader)
Crown.Size = UDim2.new(1, 0, 0, 48)
Crown.Position = UDim2.new(0, 0, 0, 6)
Crown.BackgroundTransparency = 1
Crown.Font = Enum.Font.GothamBold
Crown.Text = "👑"
Crown.TextColor3 = C.Accent
Crown.TextSize = 36

local BigTitle = Instance.new("TextLabel", CreditHeader)
BigTitle.Size = UDim2.new(1, 0, 0, 22)
BigTitle.Position = UDim2.new(0, 0, 0, 56)
BigTitle.BackgroundTransparency = 1
BigTitle.Font = Enum.Font.GothamBold
BigTitle.Text = "TLG HUB 3.0"
BigTitle.TextColor3 = C.Text
BigTitle.TextSize = 16

local SubTitle = Instance.new("TextLabel", CreditHeader)
SubTitle.Size = UDim2.new(1, 0, 0, 18)
SubTitle.Position = UDim2.new(0, 0, 0, 80)
SubTitle.BackgroundTransparency = 1
SubTitle.Font = Enum.Font.GothamSemibold
SubTitle.Text = "TLG Team"
SubTitle.TextColor3 = C.Accent
SubTitle.TextSize = 12

local CreditCard = Instance.new("Frame", CreditPage)
CreditCard.Size = UDim2.new(1, 0, 0, 90)
CreditCard.BackgroundColor3 = C.BG2
CreditCard.BorderSizePixel = 0
Instance.new("UICorner", CreditCard).CornerRadius = UDim.new(0, 6)
Instance.new("UIStroke", CreditCard).Color = C.Accent

local CreditIcon = Instance.new("TextLabel", CreditCard)
CreditIcon.Size = UDim2.new(0, 40, 0, 40)
CreditIcon.Position = UDim2.new(0, 12, 0.5, -20)
CreditIcon.BackgroundTransparency = 1
CreditIcon.Font = Enum.Font.GothamBold
CreditIcon.Text = "⚔"
CreditIcon.TextColor3 = C.Accent
CreditIcon.TextSize = 26

local CreditText = Instance.new("TextLabel", CreditCard)
CreditText.Size = UDim2.new(1, -70, 1, 0)
CreditText.Position = UDim2.new(0, 58, 0, 0)
CreditText.BackgroundTransparency = 1
CreditText.Font = Enum.Font.GothamMedium
CreditText.Text = "Este script fue hecho por el actual líder de TLG North America, GatoGold"
CreditText.TextColor3 = C.Text
CreditText.TextSize = 11
CreditText.TextWrapped = true
CreditText.TextXAlignment = Enum.TextXAlignment.Left

task.spawn(function()
    while Crown.Parent do
        TS:Create(Crown, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {TextColor3 = C.AccentGlow}):Play()
        task.wait(1.5)
        if not Crown.Parent then break end
        TS:Create(Crown, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {TextColor3 = C.Accent}):Play()
        task.wait(1.5)
    end
end)

----------------------------------------------------------------
-- FUNCIONES INTERNAS
----------------------------------------------------------------
function StartFlyInternal()
    if V3 then return end
    V3 = true
    task.spawn(function()
        local c = LP.Character or LP.CharacterAdded:Wait()
        local h = c:WaitForChild("HumanoidRootPart", 5)
        local hu = c:FindFirstChildOfClass("Humanoid")
        if not h or not hu then V3 = false; return end
        local bv = Instance.new("BodyVelocity", h)
        bv.MaxForce = Vector3.new(1e9, 1e9, 1e9); bv.Velocity = Vector3.zero
        local bg = Instance.new("BodyGyro", h)
        bg.MaxTorque = Vector3.new(1e9, 1e9, 1e9); bg.P = 9e4; bg.CFrame = h.CFrame
        hu.PlatformStand = true
        local cn
        cn = RS.RenderStepped:Connect(function()
            if not V3 or not h.Parent or not LP.Character or LP.Character ~= c then
                cn:Disconnect()
                if bv then bv:Destroy() end
                if bg then bg:Destroy() end
                if hu then hu.PlatformStand = false end
                return
            end
            if not bv or not bv.Parent then bv = Instance.new("BodyVelocity", h); bv.MaxForce = Vector3.new(1e9, 1e9, 1e9) end
            if not bg or not bg.Parent then bg = Instance.new("BodyGyro", h); bg.MaxTorque = Vector3.new(1e9, 1e9, 1e9); bg.P = 9e4 end
            if not hu.PlatformStand then hu.PlatformStand = true end
            bg.CFrame = Cam.CFrame
            local mv = Vector3.zero
            if PC then
                local ok, v3 = pcall(function() return PC:GetMoveVector() end)
                if ok and v3 then mv = (Cam.CFrame.LookVector * -v3.Z) + (Cam.CFrame.RightVector * v3.X) end
            end
            mv = Vector3.new(mv.X, 0, mv.Z)
            if VDir == 1 then mv = mv + Vector3.new(0, 1, 0) end
            if VDir == -1 then mv = mv - Vector3.new(0, 1, 0) end
            if mv.Magnitude > 0.05 then bv.Velocity = mv.Unit * VSpd else bv.Velocity = Vector3.zero end
        end)
    end)
end

function StopFlyInternal()
    V3 = false
    local c = LP.Character
    local h = c and c:FindFirstChild("HumanoidRootPart")
    local hu = c and c:FindFirstChildOfClass("Humanoid")
    if h then
        local bv = h:FindFirstChild("BodyVelocity"); if bv then bv:Destroy() end
        local bg = h:FindFirstChild("BodyGyro"); if bg then bg:Destroy() end
    end
    if hu then hu.PlatformStand = false end
end

-- FLY BUTTON FLOTANTE
local FFG = Instance.new("ScreenGui", SP)
FFG.Name = "TLG_Fly"; FFG.ResetOnSpawn = false
local FB = Instance.new("TextButton", FFG)
FB.Size = UDim2.new(0, 90, 0, 42); FB.Position = UDim2.new(0.05, 0, 0.55, 0)
FB.BackgroundColor3 = C.BG2; FB.TextColor3 = C.Accent; FB.Text = "FLY: OFF"
FB.Font = Enum.Font.GothamBold; FB.TextSize = 12; FB.Visible = false; FB.BorderSizePixel = 0
FB.AutoButtonColor = false
Instance.new("UICorner", FB).CornerRadius = UDim.new(0, 8)
Instance.new("UIStroke", FB).Color = C.Accent
local UB = Instance.new("TextButton", FFG)
UB.Size = UDim2.new(0, 52, 0, 52); UB.Position = UDim2.new(0.05, 0, 0.68, 0)
UB.BackgroundColor3 = C.Green; UB.TextColor3 = C.Text
UB.Text = "↑"; UB.Font = Enum.Font.GothamBold; UB.TextSize = 24; UB.Visible = false; UB.BorderSizePixel = 0
UB.AutoButtonColor = false
Instance.new("UICorner", UB).CornerRadius = UDim.new(0, 26)
local DB = Instance.new("TextButton", FFG)
DB.Size = UDim2.new(0, 52, 0, 52); DB.Position = UDim2.new(0.05, 60, 0.68, 0)
DB.BackgroundColor3 = C.Yellow; DB.TextColor3 = C.Text
DB.Text = "↓"; DB.Font = Enum.Font.GothamBold; DB.TextSize = 24; DB.Visible = false; DB.BorderSizePixel = 0
DB.AutoButtonColor = false
Instance.new("UICorner", DB).CornerRadius = UDim.new(0, 26)
UB.MouseButton1Down:Connect(function() VDir = 1 end)
UB.MouseButton1Up:Connect(function() VDir = 0 end)
DB.MouseButton1Down:Connect(function() VDir = -1 end)
DB.MouseButton1Up:Connect(function() VDir = 0 end)
FB.MouseButton1Click:Connect(function()
    if V3 then
        StopFlyInternal()
        FB.Text = "FLY: OFF"; FB.BackgroundColor3 = C.BG2; FB.TextColor3 = C.Accent
        UB.Visible = false; DB.Visible = false
    else
        StartFlyInternal()
        FB.Text = "FLY: ON"; FB.BackgroundColor3 = C.Accent; FB.TextColor3 = C.BG
        if FB.Visible then UB.Visible = true; DB.Visible = true end
    end
end)
MakeDraggable(FB, FB)

-- BOTÓN FLOTANTE DE FUGA
local FugaSG = Instance.new("ScreenGui", SP)
FugaSG.Name = "TLG_Fuga"
FugaSG.ResetOnSpawn = false
FugaSG.DisplayOrder = 998

local FugaBtn = Instance.new("TextButton", FugaSG)
FugaBtn.Size = UDim2.new(0, 90, 0, 42)
FugaBtn.Position = UDim2.new(0.05, 0, 0.62, 0)
FugaBtn.BackgroundColor3 = C.BG2
FugaBtn.TextColor3 = C.Accent
FugaBtn.Text = "⚡ FUGA"
FugaBtn.Font = Enum.Font.GothamBold
FugaBtn.TextSize = 12
FugaBtn.BorderSizePixel = 0
FugaBtn.AutoButtonColor = false
FugaBtn.Visible = false
Instance.new("UICorner", FugaBtn).CornerRadius = UDim.new(0, 8)
local FugaStroke = Instance.new("UIStroke", FugaBtn)
FugaStroke.Color = C.Accent
FugaStroke.Thickness = 1.5
FugaStroke.Transparency = 0.3

task.spawn(function()
    while FugaBtn.Parent do
        TS:Create(FugaStroke, TweenInfo.new(1.2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Transparency = 0}):Play()
        task.wait(1.2)
        if not FugaBtn.Parent then break end
        TS:Create(FugaStroke, TweenInfo.new(1.2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Transparency = 0.4}):Play()
        task.wait(1.2)
    end
end)

MakeDraggable(FugaBtn, FugaBtn)

FugaBtn.MouseButton1Click:Connect(function()
    local c = LP.Character
    local h = c and c:FindFirstChild("HumanoidRootPart")
    if h then 
        h.CFrame = h.CFrame + Vector3.new(0, EH, 0)
        FugaBtn.BackgroundColor3 = C.Accent
        FugaBtn.TextColor3 = C.BG
        task.delay(0.15, function()
            FugaBtn.BackgroundColor3 = C.BG2
            FugaBtn.TextColor3 = C.Accent
        end)
    end
end)

-- HITBOX CELESTE
function StartCeleste()
    if HBCelesteConn then return end
    local function apply()
        for _, plr in ipairs(P:GetPlayers()) do pcall(function()
            if plr == LP then return end
            local c = plr.Character
            local h = c and c:FindFirstChild("HumanoidRootPart")
            if not h or not h:IsA("Part") then return end
            h.Size = Vector3.new(HBCelesteSize, HBCelesteSize, HBCelesteSize)
            h.Transparency = 0.5; h.Color = C.Accent
            h.Material = Enum.Material.Neon; h.Shape = Enum.PartType.Ball; h.CanCollide = false
        end) end
    end
    apply()
    HBCelesteConn = task.spawn(function()
        while HBCelesteOn do
            task.wait(0.5)
            apply()
        end
    end)
end

function StopCeleste()
    HBCelesteOn = false; HBCelesteConn = nil
    for _, plr in ipairs(P:GetPlayers()) do pcall(function()
        local c = plr.Character
        local h = c and c:FindFirstChild("HumanoidRootPart")
        if not h or not h:IsA("Part") then return end
        h.Size = Vector3.new(2, 2, 1); h.Transparency = 1
        h.Shape = Enum.PartType.Block; h.CanCollide = false
        h.Material = Enum.Material.Plastic; h.Color = Color3.fromRGB(255, 255, 0)
    end) end
end

-- HITBOX GIGANTE
function StartGiant()
    if HBGiantConn then return end
    local function apply()
        for _, plr in ipairs(P:GetPlayers()) do pcall(function()
            if plr == LP then return end
            local c = plr.Character
            local h = c and c:FindFirstChild("HumanoidRootPart")
            if not h or not h:IsA("Part") then return end
            if not HBGiantCache[plr] then
                HBGiantCache[plr] = {Size=h.Size, Transparency=h.Transparency, CanCollide=h.CanCollide, Shape=h.Shape, Material=h.Material, Color=h.Color}
            end
            h.Size = Vector3.new(HBGiantSize, HBGiantSize, HBGiantSize)
            h.Transparency = 1; h.CanCollide = false; h.Shape = Enum.PartType.Block
        end) end
    end
    apply()
    HBGiantConn = task.spawn(function()
        while HBGiantOn do
            task.wait(0.5)
            apply()
        end
    end)
end

function StopGiant()
    HBGiantOn = false; HBGiantConn = nil
    for _, plr in ipairs(P:GetPlayers()) do pcall(function()
        local c = plr.Character
        local h = c and c:FindFirstChild("HumanoidRootPart")
        if not h or not h:IsA("Part") then return end
        local o = HBGiantCache[plr]
        if o then
            h.Size = o.Size; h.Transparency = o.Transparency; h.CanCollide = o.CanCollide
            h.Shape = o.Shape; h.Material = o.Material; h.Color = o.Color
        else
            h.Size = Vector3.new(2, 2, 1); h.Transparency = 1; h.CanCollide = false
            h.Shape = Enum.PartType.Block
        end
    end) end
    HBGiantCache = {}
end

P.PlayerRemoving:Connect(function(plr) HBGiantCache[plr] = nil end)

-- GOD ORB
function StopGO()
    GOT = nil; GOang = 0
    if GOC then GOC:Disconnect(); GOC = nil end
end

function StartGO(plr)
    GOT = plr; GOang = 0
    if GOC then GOC:Disconnect() end
    GOC = RS.Heartbeat:Connect(function(dt)
        if not GOT then return end
        local tc = GOT.Character; local mc = LP.Character
        local th = tc and tc:FindFirstChild("HumanoidRootPart")
        local mh = mc and mc:FindFirstChild("HumanoidRootPart")
        local thu = tc and tc:FindFirstChildOfClass("Humanoid")
        if not th or not mh or not thu or thu.Health <= 0 then StopGO(); return end
        GOang = GOang + GOs * dt
        if GOang > math.pi * 2 then GOang = GOang - math.pi * 2 end
        local off = Vector3.new(math.cos(GOang) * GOd, GOh, math.sin(GOang) * GOd)
        mh.CFrame = CFrame.new(th.Position + off, th.Position) * CFrame.Angles(0, math.pi, 0)
    end)
end

P.PlayerAdded:Connect(function() task.wait(0.1); PopulatePlayerList() end)
P.PlayerRemoving:Connect(function() task.wait(0.1); PopulatePlayerList() end)
PopulatePlayerList()

RS.RenderStepped:Connect(function()
    if SHon then
        local h = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
        if h then h.WalkSpeed = SHv end
    end
end)

----------------------------------------------------------------
-- 🎯 BOTÓN ABRIR/CERRAR MENÚ
----------------------------------------------------------------
local MenuSG = Instance.new("ScreenGui", SP)
MenuSG.Name = "TLG_MenuToggle"
MenuSG.ResetOnSpawn = false
MenuSG.DisplayOrder = 999

local MenuBtn = Instance.new("TextButton", MenuSG)
MenuBtn.Size = UDim2.new(0, 48, 0, 48)
MenuBtn.Position = UDim2.new(0.02, 0, 0.05, 0)
MenuBtn.BackgroundColor3 = C.Accent
MenuBtn.TextColor3 = C.BG
MenuBtn.Text = "✕"
MenuBtn.Font = Enum.Font.GothamBold
MenuBtn.TextSize = 20
MenuBtn.BorderSizePixel = 0
MenuBtn.AutoButtonColor = false
MenuBtn.ZIndex = 100
Instance.new("UICorner", MenuBtn).CornerRadius = UDim.new(0, 24)
local MBStroke = Instance.new("UIStroke", MenuBtn)
MBStroke.Color = C.Text
MBStroke.Thickness = 2
MBStroke.Transparency = 0.5

MakeDraggable(MenuBtn, MenuBtn)

local MenuOpen = true

MenuBtn.MouseButton1Click:Connect(function()
    if MenuOpen then
        MenuOpen = false
        TS:Create(Window, TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Size = UDim2.new(0, 0, 0, 0), Position = UDim2.new(0.5, 0, 0.5, 0)}):Play()
        MenuBtn.Text = "☰"
        MenuBtn.BackgroundColor3 = C.BG2
        MenuBtn.TextColor3 = C.Accent
    else
        MenuOpen = true
        Window.Size = UDim2.new(0, 0, 0, 0)
        Window.Position = UDim2.new(0.5, 0, 0.5, 0)
        TS:Create(Window, TweenInfo.new(0.45, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = W_SIZE, Position = W_POS}):Play()
        MenuBtn.Text = "✕"
        MenuBtn.BackgroundColor3 = C.Accent
        MenuBtn.TextColor3 = C.BG
    end
end)

CloseBtn.MouseButton1Click:Connect(function()
    if MenuOpen then
        MenuOpen = false
        TS:Create(Window, TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Size = UDim2.new(0, 0, 0, 0), Position = UDim2.new(0.5, 0, 0.5, 0)}):Play()
        MenuBtn.Text = "☰"
        MenuBtn.BackgroundColor3 = C.BG2
        MenuBtn.TextColor3 = C.Accent
    end
end)

----------------------------------------------------------------
-- ▶ ANIMACIÓN DE APERTURA
----------------------------------------------------------------
Window.Size = UDim2.new(0, 0, 0, 0)
Window.Position = UDim2.new(0.5, 0, 0.5, 0)
task.wait(0.05)
TS:Create(Window, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size=W_SIZE, Position=W_POS}):Play()

print("[TLG HUB 3.0] Compact cargado ✓")
